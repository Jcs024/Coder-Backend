# Puerto en el que se levanta el servidor
PORT=8080

# URI de conexión a MongoDB Atlas
# Reemplazar <usuario>, <password>, <cluster> y <base> con los datos propios
MONGO_URI=mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/<base>

# Clave secreta para sesiones o JWT (si más adelante agregás auth)
SECRET_KEY=mi_clave_secreta
